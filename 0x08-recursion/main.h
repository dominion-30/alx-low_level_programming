/*prototype for function that writes a string followed by a new line*/

void _puts_recursion(char *s);

/*prototype for function that writes c to stdout*/
int _putchar(char c);

/*prototype for function that prints a string in reverse*/
void _print_rev_recursion(char *s);

/*prototype for function that returns the length of a string*/
int _strlen_recursion(char *s);

/* prototype for function that returns the factorial of a given number*/
int factorial(int n);

/* prototype for function that that returns the natural square root of a number*/

int _sqrt_recursion(int n);

/* prototype of function that returns the value of x raised to the power of y */
int _pow_recursion(int x, int y);
