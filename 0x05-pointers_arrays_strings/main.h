/* prototype for function that takes a pointer to an int as parameter and updates the value it points to to 98*/
void reset_to_98(int *n);


/* prototype for function that swaps the value of two integers*/
void swap_int(int *a, int *b);

/* prototype for function that tells the length  of a string*/

int _strlen(char *s);

/* prototype for function that prints a string, followed by a new line to stdout*/

void _puts(char *str);

/* prototype for function that prints character c to stdout*/

int _putchar(char c);


/* prototype for function that prints a string in reverse*/

void print_rev(char *s);

/* prototype for function that reverses a string*/
void rev_string(char *s);

/* prototype to function that prints every other character of a string, starting from the first character*/

void puts2(char *str);

/* prototype to function that prints half of a string, followed by a newline*/

void puts_half(char *str);
