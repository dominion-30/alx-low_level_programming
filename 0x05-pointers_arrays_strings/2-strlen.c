#include "main.h"
#include <string.h>
/**
* _strlen - function that returns the length of a string
*
* Return: Always int value of length of string
*/
int _strlen(char *s)

{	
	
	int len;


	len = strlen(s);
	return (len);


}
