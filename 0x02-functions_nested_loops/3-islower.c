#include <ctype.h>
/**
* int _islower(int c) checks for lowercase characters
*
* Return: Always 0 success
*/

int _islower(int c)
{	
	int result;
	if islower(c) > 0
	{	result = 1;
		return (result);
	}
	else if islower(c) == 0

	{	result = 0;
		return(result); 
	}
	

}
