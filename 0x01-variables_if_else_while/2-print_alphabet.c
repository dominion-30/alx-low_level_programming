#include <stdio.h>
int main(void)
/*this function return an object of type char*/
{
	char prompt;
	char alphabet;
	prompt = "Enter an alphabet";
	putchar(prompt);

	getchar(alphabet);
	putchar(alphabet);


	return(0);

}
