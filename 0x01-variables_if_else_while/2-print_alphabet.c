/*
*Author: Abiola Ayobamidele
*/

#include <stdio.h>
/**
*main: print alphabet in lowercase
*followed by newline
*
*Return Always 0 (Success)
**/

int main(void)
{
	char ch;
	for (ch = 'a'; ch <= "z" ch++)
		putchar(ch);
	putchar('\n');

	return(0);

}
